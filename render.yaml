services:
  - type: web
    name: gymiq-api
    runtime: node
    region: oregon
    plan: free
    branch: main
    buildCommand: npm install --include=dev && packages/database/node_modules/.bin/prisma generate --schema=packages/database/prisma/schema.prisma && npm run build --workspace=packages/shared || true && npm run build --workspace=apps/api
    startCommand: packages/database/node_modules/.bin/prisma migrate deploy --schema=packages/database/prisma/schema.prisma && node apps/api/dist/apps/api/src/index.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: REDIS_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: SMTP_USER
        sync: false
      - key: SMTP_PASS
        sync: false

[phases.install]
cmds = [
  "npm install",
  "rm -rf apps/api/node_modules/.prisma apps/api/node_modules/@prisma/client",
  "cd packages/database && npx prisma generate"
]

[phases.build]
cmds = [
  "npm run build --workspace=packages/shared || true",
  "npm run build --workspace=apps/api"
]

[start]
cmd = "cd packages/database && npx prisma migrate deploy --schema=prisma/schema.prisma && cd ../.. && node apps/api/dist/apps/api/src/index.js"

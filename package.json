{
  "name": "gymiq",
  "version": "0.1.0",
  "private": true,
  "description": "GymIQ — AI-powered retention agent for independent gyms",
  "scripts": {
    "dev": "cd unified && npm run dev",
    "build": "cd unified && npm run build",
    "lint": "cd unified && npm run lint",
    "typecheck": "cd unified && npm run typecheck",
    "start": "cd unified && npm run start"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
